import Box from '@mui/material/Box';
import { styled } from '@mui/material';

const FlexStyle = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
});

export default FlexStyle;